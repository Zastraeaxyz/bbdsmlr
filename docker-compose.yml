services:
  bbdmslr:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    volumes:
      - drizzle-data:/app/drizzle/data
    restart: unless-stopped

volumes:
  drizzle-data:
